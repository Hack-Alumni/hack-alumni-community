# =============================================================================
# HACK ALUMNI COMMUNITY - ENVIRONMENT VARIABLES
# =============================================================================
#
# This file contains all environment variables needed to run the Hack Alumni
# Community application. Copy this file to .env and fill in your values.
#
# DATABASE ARCHITECTURE:
# - Supabase provides the PostgreSQL database
# - DATABASE_URL connects directly to Supabase's PostgreSQL instance
# - SUPABASE_URL is used for Supabase-specific features (Auth, Edge Functions)
# - Both point to the same database but serve different purposes
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Direct PostgreSQL connection to Supabase database
# Format: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
# Get this from: Supabase Dashboard → Settings → Database → Connection string (URI)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Supabase project URL (for Auth, Edge Functions, etc.)
# Format: https://[PROJECT-REF].supabase.co
# Get this from: Supabase Dashboard → Settings → API
SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co

# Supabase anonymous key (for client-side operations)
# Get this from: Supabase Dashboard → Settings → API → anon public
SUPABASE_ANON_KEY=[YOUR-SUPABASE-ANON-KEY]

# =============================================================================
# BACKGROUND JOB PROCESSING (HYBRID SYSTEM)
# =============================================================================

# Upstash QStash - For real-time job processing
# Get these from: https://console.upstash.com/qstash
UPSTASH_QSTASH_TOKEN=[YOUR-QSTASH-TOKEN]
UPSTASH_QSTASH_CURRENT_SIGNING_KEY=[YOUR-QSTASH-SIGNING-KEY]
UPSTASH_QSTASH_NEXT_SIGNING_KEY=[YOUR-QSTASH-NEXT-SIGNING-KEY]

# Upstash Redis - For caching and session storage
# Get these from: https://console.upstash.com/redis
UPSTASH_REDIS_REST_URL=https://[YOUR-REDIS-INSTANCE].upstash.io
UPSTASH_REDIS_REST_TOKEN=[YOUR-REDIS-TOKEN]

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================

# Google OAuth (for login)
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=[YOUR-GOOGLE-CLIENT-ID]
GOOGLE_CLIENT_SECRET=[YOUR-GOOGLE-CLIENT-SECRET]

# GitHub OAuth (for login)
# Get these from: https://github.com/settings/developers
GITHUB_CLIENT_ID=[YOUR-GITHUB-CLIENT-ID]
GITHUB_CLIENT_SECRET=[YOUR-GITHUB-CLIENT-SECRET]

# JWT and Session secrets (generate secure random strings)
JWT_SECRET=[GENERATE-SECURE-RANDOM-STRING]
SESSION_SECRET=[GENERATE-SECURE-RANDOM-STRING]

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# API URL (your Vercel deployment URL)
# For development: http://localhost:8080
# For production: https://your-app.vercel.app
API_URL=https://[YOUR-VERCEL-APP].vercel.app

# Environment (development/production)
ENVIRONMENT=production

# =============================================================================
# EXTERNAL SERVICES
# =============================================================================

# Sentry (error tracking)
# Get this from: https://sentry.io/settings/projects/[PROJECT]/keys/
SENTRY_DSN=[YOUR-SENTRY-DSN]

# Mixpanel (analytics)
# Get this from: https://mixpanel.com/settings/project/token
MIXPANEL_TOKEN=[YOUR-MIXPANEL-TOKEN]

# =============================================================================
# FILE STORAGE (Cloudflare R2)
# =============================================================================

# Cloudflare R2 credentials for file uploads (resumes, profile pictures, etc.)
# Get these from: https://dash.cloudflare.com/profile/api-tokens
R2_ACCESS_KEY_ID=[YOUR-R2-ACCESS-KEY]
R2_SECRET_ACCESS_KEY=[YOUR-R2-SECRET-KEY]
R2_ACCOUNT_ID=[YOUR-R2-ACCOUNT-ID]
R2_BUCKET_NAME=[YOUR-R2-BUCKET-NAME]

# =============================================================================
# SLACK INTEGRATION
# =============================================================================

# Slack app credentials
# Get these from: https://api.slack.com/apps/[YOUR-APP]/general
SLACK_BOT_TOKEN=[YOUR-SLACK-BOT-TOKEN]
SLACK_SIGNING_SECRET=[YOUR-SLACK-SIGNING-SECRET]

# =============================================================================
# EMAIL (Postmark for production)
# =============================================================================

# Postmark API token for sending emails
# Get this from: https://account.postmarkapp.com/api_tokens
POSTMARK_API_TOKEN=[YOUR-POSTMARK-API-TOKEN]

# =============================================================================
# OPTIONAL EXTERNAL INTEGRATIONS
# =============================================================================
# These are optional and only needed if you want to use specific features

# Airtable integration
# AIRTABLE_API_KEY=[YOUR-AIRTABLE-API-KEY]
# AIRTABLE_FAMILY_BASE_ID=[YOUR-AIRTABLE-BASE-ID]
# AIRTABLE_MEMBERS_TABLE_ID=[YOUR-AIRTABLE-TABLE-ID]

# AI/ML services
# ANTHROPIC_API_KEY=[YOUR-ANTHROPIC-API-KEY]
# OPENAI_API_KEY=[YOUR-OPENAI-API-KEY]
# COHERE_API_KEY=[YOUR-COHERE-API-KEY]
# PINECONE_API_KEY=[YOUR-PINECONE-API-KEY]

# Marketing/Communication
# MAILCHIMP_API_KEY=[YOUR-MAILCHIMP-API-KEY]
# MAILCHIMP_AUDIENCE_ID=[YOUR-MAILCHIMP-AUDIENCE-ID]
# MAILCHIMP_SERVER_PREFIX=[YOUR-MAILCHIMP-SERVER-PREFIX]

# SMS (Twilio)
# TWILIO_ACCOUNT_SID=[YOUR-TWILIO-ACCOUNT-SID]
# TWILIO_AUTH_TOKEN=[YOUR-TWILIO-AUTH-TOKEN]
# TWILIO_PHONE_NUMBER=[YOUR-TWILIO-PHONE-NUMBER]

# =============================================================================
# DEVELOPMENT ONLY (not needed for production)
# =============================================================================

# Local development settings (override with your local setup)
# STUDENT_PROFILE_URL=http://localhost:3000
# REDIS_URL=redis://localhost:6380

# SMTP for local email testing
# SMTP_HOST=[YOUR-SMTP-HOST]
# SMTP_USERNAME=[YOUR-SMTP-USERNAME]
# SMTP_PASSWORD=[YOUR-SMTP-PASSWORD]
